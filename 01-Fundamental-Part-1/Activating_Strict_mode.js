// Strict mode is a special mode that we can activate in JavaScript, which makes easier for us to write a secure JavaScript code.
// To activate Strict mode is to write this ring at the begining of the script : 'use strict';


'use strict';

let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriversLicense = true;
if (hasDriversLicense) console.log('I can drive');

// const interface = 'Audio';
//const private = 534;
// Strict mode reserves word that we cannot then for variabels.